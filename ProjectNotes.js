// In mongodb the findOne() also known as find().limit(1) method scans the entire database  and returns the response according to the search criteria.
// A refresh token should actually be long lived comapred to an access token.
